import { Item } from './item';
export declare abstract class Operator implements Item {
    protected operatorKeyWord: string;
    protected items: Item[];
    protected constructor(operatorKeyWord: string, items: Item[]);
    toString(): string;
}
export declare class And extends Operator {
    constructor(items: Item[]);
}
export declare class Or extends Operator {
    constructor(items: Item[]);
}
export declare class Not extends Operator {
    constructor(item: Item);
    toString(): string;
}
export declare class Exists extends Operator {
    constructor(item: Item);
}
